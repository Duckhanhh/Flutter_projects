import 'package:flutter/material.dart';

class PlacesList extends StatelessWidget {
  const PlacesList({super.key, required List places});
  @override
  Widget build(BuildContext context) {
    throw UnimplementedError();
  }
}
